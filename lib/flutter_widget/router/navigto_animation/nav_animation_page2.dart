import 'package:flutter/material.dart';

class NavAnimationPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('界面间过度动画'),
      ),
      body: Center(
        child: Text('界面过度动画'),
      ),
    );
  }
}
